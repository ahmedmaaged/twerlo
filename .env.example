# .env.example

# === OPENAI-COMPATIBLE API CONFIGURATION ===
# IMPORTANT: LLM and Embedding providers can be completely different!
# Each provider can have its own API key and base URL

# === LLM PROVIDER SETTINGS (OpenAI-Compatible) ===
# API key for language model provider
LLM_API_KEY=your_llm_api_key_here
# Base URL for language model API (change to switch providers instantly)
LLM_BASE_URL=https://api.openai.com/v1
# Model name (provider-specific)
LLM_MODEL_NAME=gpt-4o-mini
# Response configuration
LLM_MAX_TOKENS=500
LLM_TEMPERATURE=0.7

# === EMBEDDING PROVIDER SETTINGS (OpenAI-Compatible) ===
# API key for embedding provider (can be different from LLM)
EMBEDDING_API_KEY=your_embedding_api_key_here
# Base URL for embedding API (can be different from LLM provider)
EMBEDDING_BASE_URL=https://api.openai.com/v1
# Embedding model name
EMBEDDING_MODEL_NAME=text-embedding-3-small
# Vector dimensions (must match model)
EMBEDDING_DIMENSIONS=1536

# === EXAMPLE: MIXED PROVIDERS ===
# LLM from Anthropic, Embeddings from OpenAI:
# LLM_API_KEY=your_anthropic_api_key
# LLM_BASE_URL=https://api.anthropic.com/v1
# LLM_MODEL_NAME=claude-3-sonnet-20240229
# EMBEDDING_API_KEY=your_openai_api_key
# EMBEDDING_BASE_URL=https://api.openai.com/v1
# EMBEDDING_MODEL_NAME=text-embedding-3-small

# === EXAMPLE: LOCAL + CLOUD HYBRID ===
# Local LLM + Cloud Embeddings:
# LLM_API_KEY=not_needed_for_local
# LLM_BASE_URL=http://localhost:11434/v1
# LLM_MODEL_NAME=llama2:latest
# EMBEDDING_API_KEY=your_openai_api_key
# EMBEDDING_BASE_URL=https://api.openai.com/v1

# === JWT Configuration ===
JWT_SECRET_KEY=your_super_secret_jwt_key_here_make_it_long_and_random
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# === DATABASE CONFIGURATION ===
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=twerlo_db

# === VECTOR DATABASE ===
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=documents

# === APPLICATION SETTINGS ===
APP_HOST=0.0.0.0
APP_PORT=8000
MAX_FILE_SIZE_MB=10
